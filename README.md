# DAA-lab-B125116
Tools Used:-

1. Programming Language:-
C Programming Language
Used to implement all algorithms and simulations.

3. Code Editor / IDE
Visual Studio Code (VS Code)

4. Compiler

The compiler converts the C source code into machine code.

Used compilers :

GCC (GNU Compiler Collection) (Linux/macOS/MinGW on Windows)
Clang (macOS)

Example:
gcc program.c -o program

4. Terminal / Command Prompt

Used to compile and execute the program.
macOS: Terminal



Libraries Used in the Lab Programs:-

Header File	Purpose
stdio.h	  Input and output (printf, scanf).
stdlib.h	Random numbers (rand), memory allocation, utility functions.
time.h    Seed random number generator using time(NULL).

time.h	Seed random number generator using time(NULL)
